services:
  app:
    container_name: template-next-app
    ports: !reset []
    expose:
      - "3000"
    networks:
      - default
      - web

  mysql:
    ports: !reset []

networks:
  default:
  web:
    external: true
